# Ultralytics YOLO 🚀, AGPL-3.0 license+卷积线性融合Transformer (CLFT) 模块，旨在利用卷积和Transformer特性来增强小目标检测能力
# YOLO11 object detection model with P3-P5 outputs. For Usage examples see https://docs.ultralytics.com/tasks/detect

# Parameters
nc: 1 # number of classes
scales: # model compound scaling constants, i.e. 'model=yolo11n.yaml' will call yolo11.yaml with scale 'n'
  # [depth, width, max_channels]
  n: [0.50, 0.25, 1024] # summary: 319 layers, 2624080 parameters, 2624064 gradients, 6.6 GFLOPs
  s: [0.50, 0.50, 1024] # summary: 319 layers, 9458752 parameters, 9458736 gradients, 21.7 GFLOPs
  m: [0.50, 1.00, 512] # summary: 409 layers, 20114688 parameters, 20114672 gradients, 68.5 GFLOPs
  l: [1.00, 1.00, 512] # summary: 631 layers, 25372160 parameters, 25372144 gradients, 87.6 GFLOPs
  x: [1.00, 1.50, 512] # summary: 631 layers, 56966176 parameters, 56966160 gradients, 196.0 GFLOPs

backbone:
  # Backbone配置保持不变
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]] # 9
  - [-1, 2, C2PSA, [1024]] # 10

head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 11 - 上采样 P4
  - [-1, 1, Conv, [256, 3, 1]]  # 将 P4 特征图调整至与 P3 通道数一致
  - [[11, 3], 1, Concat, [1]]  # 将 P4 和 P3 特征结合
  - [-1, 1, C3k2, [512, False]]  # 使用 C3k2 增强特征

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 上采样至 P2 尺寸
  - [-1, 1, Conv, [256, 3, 1]]  # 调整通道数以匹配 P2
  - [[-1, 1], 1, Concat, [1]]  # 将上采样后的特征与 P2 特征结合
  - [-1, 1, CLFT, [256, 256, 4096, 64]]  # 使用 CLFT 增强 P2

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]  # 上采样至 P1 尺寸
  - [-1, 1, Conv, [128, 3, 1]]  # 调整通道数以匹配 P1
  - [[-1, 0], 1, Concat, [1]]  # 将上采样后的特征与 P1 特征结合
  - [-1, 1, CLFT, [128, 128, 16384, 32]]  # 使用 CLFT 增强 P1

  - [[3, 7, 10, 13], 1, Detect, [nc]]  # Detect，将 P1、P2、P3 和 P4 结合用于检测


  - [[13, 16, 19, 10], 1, Detect, [nc]]  # Detect，将 P1、P2、P3 和 P4 结合用于检测














